{
  "scripts": {
    "producer": "ts-node-dev --transpile-only --ignore-watch node_modules src/producer/index.ts",
    "consumer1": "ts-node-dev --transpile-only --ignore-watch node_modules src/consumer/start1.ts",
    "consumer2": "ts-node-dev --transpile-only --ignore-watch node_modules src/consumer/start2.ts"
  },
  "dependencies": {
    "kafkajs": "^2.0.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.3"
  }
}
